(function ($) {

	"use strict";

	$(document).ready(function($) {

	 $('.accessible-slick-slider--quote .accessible-slick-slider__slides').slick({
		  slidesToShow: 1,
		  dots: false,
		  arrows: true,
		  centerMode: true,
		  centerPadding: 0,
		  infinite: true
	  });
	})

})(jQuery);
